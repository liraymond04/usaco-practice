#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    int n;
    cin >> n;

    vector<vector<int> > adj(n, vector<int>(n));

    vector<int> hairs(n);
    for (int i=0; i<n; i++) {
        cin >> hairs[i];
    }
    
    return 0;
}